# BGE嵌入服务依赖包 - 仅使用ModelScope，不依赖HuggingFace
# 使用清华镜像源: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

flask==2.3.3
modelscope
numpy==1.24.3
torch==2.0.1
transformers==4.33.2
tokenizers==0.13.3
safetensors==0.3.3 